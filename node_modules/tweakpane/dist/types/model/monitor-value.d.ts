import { Emitter, EventTypeMap } from '../misc/emitter';
/**
 * @hidden
 */
export interface MonitorValueEvents<T> extends EventTypeMap {
    update: {
        rawValue: T;
        sender: MonitorValue<T>;
    };
}
/**
 * @hidden
 */
export declare class MonitorValue<T> {
    readonly emitter: Emitter<MonitorValueEvents<T>>;
    private rawValues_;
    private bufferSize_;
    constructor(bufferSize: number);
    get rawValues(): T[];
    get bufferSize(): number;
    append(rawValue: T): void;
}
