import { Emitter, EventTypeMap } from '../misc/emitter';
/**
 * @hidden
 */
export interface DisposableEvents extends EventTypeMap {
    dispose: {
        sender: Disposable;
    };
}
/**
 * @hidden
 */
export declare class Disposable {
    readonly emitter: Emitter<DisposableEvents>;
    private disposed_;
    constructor();
    get disposed(): boolean;
    dispose(): boolean;
}
