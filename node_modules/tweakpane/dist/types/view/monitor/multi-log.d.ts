import { Formatter } from '../../formatter/formatter';
import { MonitorValue } from '../../model/monitor-value';
import { View, ViewConfig } from '../view';
import { MonitorView } from './monitor';
interface Config<T> extends ViewConfig {
    formatter: Formatter<T>;
    lineCount: number;
    value: MonitorValue<T>;
}
/**
 * @hidden
 */
export declare class MultiLogMonitorView<T> extends View implements MonitorView<T> {
    readonly value: MonitorValue<T>;
    private formatter_;
    private textareaElem_;
    constructor(document: Document, config: Config<T>);
    update(): void;
    private onValueUpdate_;
}
export {};
