import { Color } from '../../model/color';
import { InputValue } from '../../model/input-value';
import { PickedColor } from '../../model/picked-color';
import { View, ViewConfig } from '../view';
import { InputView } from './input';
interface Config extends ViewConfig {
    pickedColor: PickedColor;
}
declare type HtmlInputElements3 = [HTMLInputElement, HTMLInputElement, HTMLInputElement];
/**
 * @hidden
 */
export declare class ColorComponentTextsInputView extends View implements InputView<Color> {
    readonly pickedColor: PickedColor;
    readonly modeSelectElement: HTMLSelectElement;
    private inputElems_;
    constructor(document: Document, config: Config);
    get inputElements(): HtmlInputElements3;
    get value(): InputValue<Color>;
    update(): void;
    private onValueChange_;
}
export {};
