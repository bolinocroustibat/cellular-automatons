import { ListItem } from '../../constraint/list';
import { InputValue } from '../../model/input-value';
import { View, ViewConfig } from '../view';
import { InputView } from './input';
interface Config<T> extends ViewConfig {
    options: ListItem<T>[];
    stringifyValue: (value: T) => string;
    value: InputValue<T>;
}
/**
 * @hidden
 */
export declare class ListInputView<T> extends View implements InputView<T> {
    readonly value: InputValue<T>;
    private selectElem_;
    private stringifyValue_;
    constructor(document: Document, config: Config<T>);
    get selectElement(): HTMLSelectElement;
    update(): void;
    private onValueChange_;
}
export {};
