import { InputValue } from '../../model/input-value';
import { ViewModel } from '../../model/view-model';
import { SliderInputView } from '../../view/input/slider';
import { InputController } from './input';
interface Config {
    value: InputValue<number>;
    viewModel: ViewModel;
}
/**
 * @hidden
 */
export declare class SliderInputController implements InputController<number> {
    readonly viewModel: ViewModel;
    readonly value: InputValue<number>;
    readonly view: SliderInputView;
    private maxValue_;
    private minValue_;
    private ptHandler_;
    private step_;
    constructor(document: Document, config: Config);
    private handlePointerEvent_;
    private onPointerDown_;
    private onPointerMove_;
    private onPointerUp_;
    private onKeyDown_;
}
export {};
