import { Color } from '../../model/color';
import { InputValue } from '../../model/input-value';
import { ViewModel } from '../../model/view-model';
import { HPaletteInputView } from '../../view/input/h-palette';
import { InputController } from './input';
interface Config {
    value: InputValue<Color>;
    viewModel: ViewModel;
}
/**
 * @hidden
 */
export declare class HPaletteInputController implements InputController<Color> {
    readonly viewModel: ViewModel;
    readonly value: InputValue<Color>;
    readonly view: HPaletteInputView;
    private ptHandler_;
    constructor(document: Document, config: Config);
    private handlePointerEvent_;
    private onPointerDown_;
    private onPointerMove_;
    private onPointerUp_;
    private onKeyDown_;
}
export {};
