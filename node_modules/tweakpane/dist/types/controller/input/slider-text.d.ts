import { Formatter } from '../../formatter/formatter';
import { InputValue } from '../../model/input-value';
import { ViewModel } from '../../model/view-model';
import { Parser } from '../../parser/parser';
import { SliderTextInputView } from '../../view/input/slider-text';
import { InputController } from './input';
interface Config {
    formatter: Formatter<number>;
    parser: Parser<string, number>;
    value: InputValue<number>;
    viewModel: ViewModel;
}
/**
 * @hidden
 */
export declare class SliderTextInputController implements InputController<number> {
    viewModel: ViewModel;
    private sliderIc_;
    private textIc_;
    private value_;
    private view_;
    constructor(document: Document, config: Config);
    get value(): InputValue<number>;
    get view(): SliderTextInputView;
}
export {};
