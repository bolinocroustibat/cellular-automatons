import { InputValue } from '../../model/input-value';
import { ViewModel } from '../../model/view-model';
import { CheckboxInputView } from '../../view/input/checkbox';
import { InputController } from './input';
/**
 * @hidden
 */
export interface Config {
    value: InputValue<boolean>;
    viewModel: ViewModel;
}
/**
 * @hidden
 */
export declare class CheckboxInputController implements InputController<boolean> {
    readonly viewModel: ViewModel;
    readonly value: InputValue<boolean>;
    readonly view: CheckboxInputView;
    constructor(document: Document, config: Config);
    private onInputChange_;
}
