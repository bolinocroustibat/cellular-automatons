import { InputValue } from '../../model/input-value';
import { ViewModel } from '../../model/view-model';
import { ListInputView } from '../../view/input/list';
import { InputController } from './input';
interface Config<T> {
    stringifyValue: (value: T) => string;
    value: InputValue<T>;
    viewModel: ViewModel;
}
/**
 * @hidden
 */
export declare class ListInputController<T> implements InputController<T> {
    readonly viewModel: ViewModel;
    private listItems_;
    private value_;
    private view_;
    constructor(document: Document, config: Config<T>);
    get value(): InputValue<T>;
    get view(): ListInputView<T>;
    private onSelectChange_;
}
export {};
