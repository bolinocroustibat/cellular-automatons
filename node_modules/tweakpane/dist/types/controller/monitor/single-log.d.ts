import { Formatter } from '../../formatter/formatter';
import { MonitorValue } from '../../model/monitor-value';
import { ViewModel } from '../../model/view-model';
import { SingleLogMonitorView } from '../../view/monitor/single-log';
import { MonitorController } from './monitor';
interface Config<T> {
    formatter: Formatter<T>;
    value: MonitorValue<T>;
    viewModel: ViewModel;
}
/**
 * @hidden
 */
export declare class SingleLogMonitorController<T> implements MonitorController<T> {
    readonly viewModel: ViewModel;
    readonly value: MonitorValue<T>;
    readonly view: SingleLogMonitorView<T>;
    constructor(document: Document, config: Config<T>);
}
export {};
