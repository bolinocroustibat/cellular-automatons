import { Color } from '../model/color';
import { Formatter } from './formatter';
/**
 * @hidden
 */
export declare class ColorFormatter implements Formatter<Color> {
    private stringifier_;
    constructor(stringifier: (color: Color) => string);
    format(value: Color): string;
}
