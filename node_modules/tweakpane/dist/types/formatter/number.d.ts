import { Formatter } from './formatter';
/**
 * @hidden
 */
export declare class NumberFormatter implements Formatter<number> {
    private digits_;
    constructor(digits: number);
    get digits(): number;
    format(value: number): string;
}
